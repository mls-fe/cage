{
  "name": "cage",
  "version": "2.4.0",
  "description": "whornbill environment utility.",
  "bin": "bin/index.js",
  "keywords": [
    "cli",
    "build",
    "whornbill"
  ],
  "scripts": {
    "build": "babel src -w -d build",
    "dist": "babel src -d dist"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/mls-fe/cage.git"
  },
  "engines": {
    "node": ">=4.4.7"
  },
  "dependencies": {
    "bluebird": "^3.5.0",
    "cfonts": "^1.1.1",
    "commander": "^2.9.0",
    "configstore": "^3.0.0",
    "inquirer": "^3.0.6",
    "rimraf": "^2.6.1"
  },
  "devDependencies": {
    "babel-core": "^6.24.0",
    "babel-plugin-transform-async-to-module-method": "^6.7.0",
    "babel-preset-async-to-bluebird": "^1.0.0",
    "babel-preset-env": "^1.2.1"
  }
}
